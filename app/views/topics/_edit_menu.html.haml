-entity = topic 
=company
=entity
-if false
  .btn-group.pull-right    
    %a.btn-mini.dropdown-toggle(data-toggle="dropdown" href="#")
      %i.icon-cog
      %span.caret
    %ul.dropdown-menu
      %li=link_to "Topics", topics_path
      %li=link_to "Companies", companies_path
      %li= link_to 'Edit', path_for(entity, :action=>"edit")
      - if entity.active?
        %li=link_to "Inactivate", path_for(entity, :action=>"update_state", :state=>"inactive")
      -else
        %li=link_to "Inactivate", path_for(entity, :action=>"update_state", :state=>"active")
      