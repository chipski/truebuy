-#.sliderContainer.fullWidth.clearfix
#full-width-slider.royalSlider.heroSlider.rsMinW
  .rsContent
    .rsImg=image_tag(@company.cover_url("medium"), :class=>"rsImg")
    .infoBlock.infoBlockLeftBlack.rsABlock{:data=>{:fade=>{:effect=>10}, :move=>{:offset=>10, :effect=>"bottom"}, :speed=>"200"}} 
      %h1=@company.name
    .infoBlock.rsAbsoluteEl.rsNoDrag{:data=>{:fade=>{:effect=>10}, :move=>{:offset=>10, :effect=>"bottom"}, :speed=>"200"}}
      .blurb=@company.blurb ? @company.blurb.html_safe : ""

  -@company.slider_photos.each do |photo|
    .rsContent
      =image_tag(photo.image.medium.url, :class=>"rsImg")
      .infoBlock.rsAbsoluteEl.rsNoDrag{:data=>{:fade=>{:effect=>10}, :move=>{:offset=>10, :effect=>"bottom"}, :speed=>"200"}}
        %h1=@company.name
      .infoBlock.infoBlockLeftBlack.rsABlock.rsNoDrag{:data=>{:fade=>{:effect=>10}, :move=>{:offset=>10, :effect=>"bottom"}, :speed=>"200"}}
        %h1= photo.name
        
          
.hero
  =@company.body ? @company.body.html_safe : "" 
.row#masonry
  %input#parentID{:type => "hidden", :value => @company.id}
    -@company.topics.each do |topic|
      .card
        .img-box
          =link_to image_tag(topic.cover_url), topic_path(topic)
        .card-info
          -if is_admin?
            .card_edit
              =render :partial=>'shared/edit_menu', :locals=>{:entity=>topic}
          %h4{:style=>"text-align: center;"}
            =topic.name  
      
