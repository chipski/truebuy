# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: sqlite3
  database: db/development.sqlite3
  pool: 5
  timeout: 5000
  encoding: utf8
  
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
    adapter: mysql
    encoding: utf8
    #collation: utf8_general_ci
    reconnect: false
    database: rightbyme_test
    pool: 5
    username: root
    password:

production:
    adapter: mysql
    encoding: utf8             
    #port: 5432   
    pool: 5 
    database: rightbyme_production
    username: root
    password: 


cucumber:
  <<: *test